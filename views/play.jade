extends layout

block content
  h1= title

  form(action="/play" method="POST")

      p Enter your player name:
      input(type="text" name="name" placeholder="Player name")

      p Enter your code:
      textarea(name="code")
          | {
          |   start: function () {
          |   },
          | 
          |   end: function () {
          |   },
          | 
          |   play: function () {
          |     return "W"; // R,P,S,D,W
          |   },
          | 
          |   result: function (opponentPlayed, result) {
          |     // opponentPlayed is one of: R,P,S,D,W
          |     // result is one of: 'win', 'loss', 'tie'
          |   }
          | }

      input(type="submit")